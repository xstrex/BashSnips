#!/bin/bash
# Takes process pid as first argument
spinner () {
        PID="$1" && i=1 && sp="/-\|"
        printf " "
        while [ -d /proc/$PID ]; do
                printf "\b${sp:i++%${#sp}:1}"
        done
        printf "\n"
}
